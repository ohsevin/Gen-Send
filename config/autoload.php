<?php

/* used to autoload helpers! */

$autoload['helpers'] = array(
	/* $key => $helpername
	 * 
	 * $key is the value assigned to the variable accessed via $this, if not set, it's set to strtolower($helpername);
	 * 
	 * so it'll be $this->mfyu_string for example
	 * 
	 */
	
);

$autoload['libraries'] = array(
	/* $key => $libraryname
	 * 
	 * $key is the value assigned to the variable accessed via $this, if not set, it's set to strtolower($libraryname);
	 * 
	 */
	
	'database'
	
);
